window.__CONFIG__ = {
    // for the local development environment the vite server proxies requests
    // to /api to the biosamples server
    // for docker based deployments /api
    SCHEMA_STORE_URL: "/api/v2"
};
